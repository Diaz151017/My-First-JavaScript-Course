
let result;

result = 20 + 30 * 2;// Según la jerarquía de operaciones, la multiplicación se realiza antes que la suma.

result = (20 + 30) * 2; // Si se usan paréntesis, la suma se realiza primero.

result = (20 + 30 + 40 + 50+ 60) * .2; // Se puede usar un número decimal para obtener un porcentaje.

result = (20 + 30) *1.16; // Se puede usar un número decimal para aplicar un IVA del 16%.

console.log(result);