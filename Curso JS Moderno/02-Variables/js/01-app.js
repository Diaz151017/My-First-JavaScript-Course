var producto = "Monitor 24 pulgadas";// Declaración de variable
console.log(producto); // Imprime "Monitor 24 pulgadas"

producto = "Monitor 20 pulgadas"; // Reasignación de variable
console.log(producto); // Imprime "Monitor 20 pulgadas"

producto = 20; // Reasignación de variable a un número
console.log(producto); // Imprime 20

//Javascript es un lenguaje de tipado dinámico, por lo que no es necesario declarar el tipo de dato al crear una variable.

//Se incializar sin valor y asignar después

var disponible; // Declaración de variable sin valor

disponible = true; // Asignación de valor
disponible = false; // Reasignación de valor

//Inicializar múltiples variables.
    categoria = "Computadoras",
    marca = "MSI",
    calificacion = 5; // Se pueden usar comas para declarar múltiples variables en una sola línea.

/*var 99días; // No se puede iniciar una variable con un número, pero se puede usar un número después de una letra o guion bajo.
var 01_;
var días99;
var _01;

var nombreProducto;// Camel case, se usa para variables con múltiples palabras.
var nombre_producto_categoria_precio; // Snake case, se usa para variables con múltiples palabras separadas por guiones bajos.
var NombreProducto; // Pascal case, se usa para clases en JavaScript.

//Pascal case es el método más utilizado para nombrar clases en JavaScript, mientras que camel case es común para variables y funciones.
 
*/