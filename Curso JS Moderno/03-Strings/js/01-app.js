const producto = "Monitor 20 pulgadas\"";// Crear una variable const con su string básico const producto = ""; 
//Si necesitas un string con comillas dobles, puedes usar la barra invertida (\) para escaparlas.
const producto2 = String('Monitor 24 pulgadas');// Crear una variable const con su string 2º modo

const producto3 = new String('Monitor 32 pulgadas');// Crear una variable const con su string 3º modo (poco común)


console.log(producto);
console.log(producto2);
console.log(producto3);